spring:
  application:
    name: ordering-service
  profiles:
    active:
      default
  cloud:
    config:
      uri: http://localhost:8888
---
spring:
  profiles: dev
  cloud:
    config:
      uri: http://config-service:8888